<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item> <!-- Incorrect background -->
        <shape android:shape="rectangle">
            <solid android:color="@color/reviewOptionIncorrectBackground"/>
            <corners android:radius="6dp"/>
            <stroke android:width="1dp" android:color="@color/reviewOptionIncorrectBorder"/>
        </shape>
    </item>
    <item android:left="2dp" android:right="2dp" android:top="2dp" android:bottom="2dp"> <!-- Inner darker border to indicate selection -->
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent"/>
            <corners android:radius="4dp"/>
            <stroke android:width="2dp" android:color="@color/reviewOptionIncorrectBorder"/> <!-- Darker shade of red -->
        </shape>
    </item>
</layer-list>