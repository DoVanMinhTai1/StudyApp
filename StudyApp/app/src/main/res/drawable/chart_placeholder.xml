<!-- res/drawable/chart_placeholder_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/chartPlaceholderBackground"/> <!-- Make sure this color exists -->
    <corners android:radius="8dp"/>
    <stroke android:width="1dp" android:color="@color/chartPlaceholderBorder" android:dashWidth="4dp" android:dashGap="4dp"/> <!-- Make sure these colors exist -->
</shape>